{% trans_default_domain "gateway" %}

{% macro name(gateway) %}{% spaceless %}
	{% if gateway.testing %}
		{% trans with { '%name%': gateway.service.name } %}gateway.name{% endtrans %}
	{% else %}
		{{ gateway.service.name }}
	{% endif %}
{% endspaceless %}{% endmacro %}
