{% trans_default_domain "customer" %}

{% macro name(customer) %}{% spaceless %}
	{% if gateway.testing %}
		{% trans with { '%name%': gateway.name } %}gateway.name{% endtrans %}
	{% else %}
		{{ gateway.service.name }}
	{% endif %}
{% endspaceless %}{% endmacro %}
