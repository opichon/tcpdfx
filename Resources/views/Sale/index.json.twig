{% spaceless %}
{
    "sEcho": "{{ sEcho }}",
    "iTotalRecords": {{ iTotalRecords }},
    "iTotalDisplayRecords": {{ iTotalDisplayRecords }},
    "aaData" : [
        {% for sale in sales %}
            {
                "DT_RowId": "{{ '_' ~ sale.id }}",
                "DT_RowClass": "{{ 'sale' }}",
                "0": "",
                "1": "{{ sale.cart.date|datetime('dd.MM.yy', 'HH:mm', time_zone, app.request.locale) }}",
                "2": "{{ sale.orderId }}",
                "3": "{% include 'DzangocartCoreBundle:Order:store.html.twig' with { store: sale.order.store } %}",
                "4": "{{ sale.name }}",
                "5": "{{ sale.quantity|integer(app.request.locale) }}",
                "6": "{{ sale.currencyId }}",
                "7": "{{ sale.getAmountExcl|currency(sale.currencyId, app.request.locale) }}",
                "8": "{{ sale.getTaxAmount|currency(sale.currencyId, app.request.locale)}}",
                "9": "{{ sale.getAmountIncl|currency(sale.currencyId, app.request.locale)}}",
                "10": "{% include 'DzangocartCoreBundle:Sale:actions.html.twig' %}"
            }{% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}
{% endspaceless %}
