{% spaceless %}
{
    "sEcho": "{{ sEcho }}",
    "iTotalRecords": {{ iTotalRecords }},
    "iTotalDisplayRecords": {{ iTotalDisplayRecords }},
    "aaData" : [
        {% for user in users %}
            {
                "DT_RowId": "{{ '_' ~ user.id }}",
                "DT_RowClass": "{{ 'users' }}",
                "id": "{{ user.id }}",
                "0": "",
                "1": "{{ user.id }}",
                "2": "{{ user.realm }}",
                "3": "{{ user.code }}",
                "4": "{{ user.username }}",
                "5": "{{ user.createdAt|date('d.m.Y h:i') }}",
                "6": "{{ user.lastLogin|date('d.m.Y h:i') }}",
                "7": ""
            }{% if not loop.last %},{% endif %}
        {% endfor %}
    ]
}
{% endspaceless %}
